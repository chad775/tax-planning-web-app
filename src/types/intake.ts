// TypeScript types for normalized schema
export interface IntakeData {
  // TODO: Define normalized intake schema
  // This should match the form structure from intake/page.tsx
}

export interface TaxPlanningInput {
  intake: IntakeData;
  // Additional fields as needed
}
